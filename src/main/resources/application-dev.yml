spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mall_tiny?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Taipei
    username: root
    password: root
  redis:
    host: localhost # Redis服務主機網址
    database: 0 # Redis資料庫索引（預設為0）
    port: 6379 # Redis服務port
    password: # Redis password（默认为空）
    timeout: 3000ms # 單位：毫秒

logging:
  level:
    root: info
    com.macro.mall: debug